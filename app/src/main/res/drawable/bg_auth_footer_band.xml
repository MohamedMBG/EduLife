<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The footer band anchors long forms while keeping security copy visually secondary. -->
    <gradient
        android:angle="0"
        android:startColor="@color/brand_primary_surface"
        android:endColor="@color/brand_surface_muted" />
    <corners
        android:topLeftRadius="24dp"
        android:topRightRadius="24dp" />
</shape>
