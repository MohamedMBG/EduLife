<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/teacher_background">

    <!-- Top bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/teacher_surface"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="16dp"
        android:gravity="center_vertical"
        android:elevation="2dp">

        <TextView
            android:id="@+id/cmsDetailBackButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/teacher_back_cd"
            android:textColor="@color/teacher_accent"
            android:textSize="15sp"
            android:paddingEnd="12dp"
            android:paddingVertical="8dp"
            android:contentDescription="@string/teacher_back_cd" />

        <TextView
            android:id="@+id/cmsDetailCourseTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/teacher_text_primary"
            android:textSize="17sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="Course Title" />
    </LinearLayout>

    <!-- State card (loading / error) -->
    <LinearLayout
        android:id="@+id/cmsDetailStateCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="24dp"
        android:background="@drawable/bg_teacher_course_card"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:paddingVertical="28dp"
        android:gravity="center"
        android:visibility="gone"
        tools:visibility="visible">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/cmsDetailLoadingIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:indicatorColor="@color/teacher_accent"
            app:trackColor="@color/teacher_border"
            android:indeterminate="true"
            android:visibility="gone"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/cmsDetailStateText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="@color/teacher_text_secondary"
            android:textSize="14sp"
            android:gravity="center"
            tools:text="@string/teacher_loading" />

        <TextView
            android:id="@+id/cmsDetailRetryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/bg_teacher_add_section_button"
            android:paddingHorizontal="20dp"
            android:paddingVertical="9dp"
            android:text="@string/teacher_retry"
            android:textColor="@color/teacher_accent"
            android:textSize="13sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Sections label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="8dp"
        android:text="@string/teacher_sections_title"
        android:textColor="@color/teacher_text_secondary"
        android:textSize="11sp"
        android:textStyle="bold"
        android:letterSpacing="0.10" />

    <!-- Sections list (fills remaining space) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/cmsSectionsRecycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="16dp"
        android:clipToPadding="false"
        tools:listitem="@layout/item_cms_section" />

    <!-- Add section button -->
    <TextView
        android:id="@+id/cmsAddSectionButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="@drawable/bg_teacher_add_section_button"
        android:gravity="center"
        android:paddingVertical="14dp"
        android:text="@string/teacher_add_section"
        android:textColor="@color/teacher_accent"
        android:textSize="14sp"
        android:textStyle="bold" />

</LinearLayout>
