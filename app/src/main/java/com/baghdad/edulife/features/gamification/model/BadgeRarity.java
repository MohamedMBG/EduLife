package com.baghdad.edulife.features.gamification.model;

/** Badge rarity tier used for visual styling (color, glow) in the badge grid and detail dialog. */
public enum BadgeRarity {
    COMMON,
    RARE,
    EPIC,
    LEGENDARY
}
