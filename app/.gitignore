# App module build artifacts are generated by Gradle and can include release outputs.
/build/

# Keep signed packages and mapping files out of the app module if generated manually.
*.apk
*.aab
*.idsig
mapping/

# google-services.json contains Firebase project secrets and must never be committed.
# Each developer or CI environment must supply their own copy at app/google-services.json.
google-services.json
