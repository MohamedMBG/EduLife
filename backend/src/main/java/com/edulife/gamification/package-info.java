/**
 * Gamification module — single source of truth for XP, levels, streaks, and
 * badges across web and Android. Clients render the values returned here and
 * must never compute progression locally.
 */
package com.edulife.gamification;
